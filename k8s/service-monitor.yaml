apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: work-app-sm
  namespace: lab3
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: work-app
  endpoints:
  - port: http
    path: /q/metrics
    interval: 5s
